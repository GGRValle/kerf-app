{
  "name": "@kerf/core",
  "version": "0.0.1",
  "private": true,
  "description": "Kerf W1: Blackboard Layer A + Projections API + Permission Provider",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "smoke": "tsx src/examples/smoke.ts",
    "demo:decision-card": "tsx src/examples/decision-card-demo.ts",
    "demo:decision-queue": "tsx src/examples/decision-queue-demo.ts",
    "demo:w1-queue:esbuild": "npx --yes esbuild@0.25.0 src/examples/w1-decision-queue-demo.ts --bundle --platform=browser --format=iife --outfile=src/examples/w1-decision-queue-demo.bundle.js",
    "demo:w1-queue": "npm run demo:w1-queue:esbuild && node -e \"console.log('Built src/examples/w1-decision-queue-demo.bundle.js — open src/examples/w1-decision-queue-demo.html in a browser.')\"",
    "demo:w1-queue:serve": "npm run demo:w1-queue:esbuild && node -e \"console.log('\\nKerf W1 demo: http://localhost:8000/examples/w1-decision-queue-demo.html\\n(static server; no auth, no fetch, no backend writes; Ctrl-C to stop)\\n')\" && python3 -m http.server 8000 --directory src",
    "test-fixtures:validate": "tsx -e \"import('./src/test-fixtures/index.js').then(m => { const events = m.seedWorld(); console.log('seed produced', events.length, 'events'); })\""
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.11.0"
  }
}
