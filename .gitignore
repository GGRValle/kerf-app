node_modules/
dist/
.DS_Store
src/examples/w1-decision-queue-demo.bundle.js

# Local environment + secrets — never enter canon history.
# .env.local.example is the public template (tracked); real .env.local is local-only.
.env
.env.local
.env.*.local
!.env.local.example
samples/voice-memos/

# Runner CLI persistence — JSONL event log for `npm run estimate` /
# `npm run estimate-voice` runs. Local-only operator history; never canon.
.kerf/
