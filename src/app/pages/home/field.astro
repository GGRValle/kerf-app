---
import Layout from '../../layouts/Layout.astro';
import RoleHomeSurface from '../../components/RoleHomeSurface.astro';
import { createLayoutContext } from '../../lib/createLayoutContext.js';

const { context } = createLayoutContext({ roleRoot: 'field_hand' });
---

<Layout context={context} titleKey="home.title" activeHref="/">
  <RoleHomeSurface roleRoot="field_hand" wireframeOverride="F-C1" />
</Layout>
