---
import Layout from '../../layouts/Layout.astro';import Card from '../../components/Card.astro';import { createLayoutContext } from '../../lib/createLayoutContext.js';const { id } = Astro.params;const { t } = createLayoutContext();
---
<Layout titleKey="decisions.detail.title" activeHref="/decisions"><Card eyebrow={t('route_shell.card_eyebrow')} title={t('decisions.detail.title')}><p>{t('decisions.preview.detail_body')}</p></Card></Layout>
