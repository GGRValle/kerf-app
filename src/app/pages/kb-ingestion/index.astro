---
import Layout from '../../layouts/Layout.astro';import Card from '../../components/Card.astro';import { createLayoutContext } from '../../lib/createLayoutContext.js';const { t } = createLayoutContext();
---
<Layout titleKey="kb.ingestion.title" activeHref="/kb-ingestion"><Card eyebrow={t('route_shell.card_eyebrow')} title={t('kb.ingestion.title')}><p>{t('kb.ingestion.preview.body')}</p></Card></Layout>
